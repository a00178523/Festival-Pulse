spring:
  application:
    name: Festival Pulse
  datasource:
    url: jdbc:h2:mem:festivalpulse
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
